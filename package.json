{
  "name": "interval-beeper",
  "version": "1.0.0",
  "description": "model for interval beeping",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest --coverage",
    "install": "npm run build",
    "prepare": "npm run test && npm run build"
  },
  "author": "kevin quing",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^24.9.1",
    "jest": "^24.9.0",
    "ts-jest": "^24.3.0",
    "typescript": "^3.8.3"
  }
}
